use strict;
use warnings;

$main::VERSION = 'v6.6.6';

use File::Basename qw( basename );
use Getopt::Guided qw( print_version_info processopts );

sub run ( \@ ) {
  my @argv = @{ $_[ 0 ] };

  return 2 unless processopts @argv, V => \&print_version_info, h => sub { };
  0
}

exit run @ARGV
